<!doctype html>
<title>project2-cache</title>
<h1>/project2-cache</h1>
<p><strong>Difficulty:</strong> 3 (next URL only on correct answer)</p>
<p><strong>Personalized:</strong> No.</p>
<ol>
  <li>Provide the YAML block that caches npm dependencies using <code>actions/cache@v4</code>.</li>
  <li>It must cache <code>~/.npm</code>, use <code>key: ${{ hashFiles("**/package-lock.json") }}</code>, and include a <code>restore-keys</code> prefix.</li>
  <li>POST just that YAML snippet as <code>answer</code> with <code>url</code> = <code><span class="origin"></span>/project2-cache</code>.</li>
</ol>
<script type="module">for (const el of document.querySelectorAll('.origin')) el.textContent = window.location.origin;</script>
